import React from 'react';
import Search from '../components/Search';
import {Input} from "../elements/index";

const Main = (props) => {
  return (
    <React.Fragment>
      <Search />
    </React.Fragment>
  );
}

export default Main;