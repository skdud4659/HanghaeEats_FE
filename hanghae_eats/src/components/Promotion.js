import React from 'react';
import { Grid } from '../elements';

const file = (props) => {
  return (
    <React.Fragment>
      <Grid>
        
      </Grid>
    </React.Fragment>
  );
}

export default file;